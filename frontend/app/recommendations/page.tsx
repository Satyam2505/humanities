import { StubPage } from "@/components/dashboard/StubPage";

export default function RecommendationsPage() {
  return (
    <StubPage
      title="Recommendations"
      description="A dedicated recommendations feed, with more suggestions than fit in a single banner, is planned for a later phase. For now, see the recommendation on the Overview page."
    />
  );
}
