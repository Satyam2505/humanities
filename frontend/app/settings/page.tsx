import { StubPage } from "@/components/dashboard/StubPage";

export default function SettingsPage() {
  return (
    <StubPage
      title="Settings"
      description="Account, data, and privacy controls will live here once the backend is in place. Atlas only analyzes data you choose to connect."
    />
  );
}
