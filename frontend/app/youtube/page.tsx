import { StubPage } from "@/components/dashboard/StubPage";

export default function YouTubePage() {
  return (
    <StubPage
      title="YouTube"
      description="A detailed, platform-specific breakdown for YouTube, covering individual videos, channels, and comment-level evidence, is coming in a later phase. For now, YouTube data powers the sample analysis on the Overview page."
    />
  );
}
