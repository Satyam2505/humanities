import { StubPage } from "@/components/dashboard/StubPage";

export default function TrendsPage() {
  return (
    <StubPage
      title="Trends"
      description="Week-over-week trend tracking across dimensions and platforms is planned for a later phase, once there's enough historical data to chart."
    />
  );
}
